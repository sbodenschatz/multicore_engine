cmake_minimum_required (VERSION 2.6)
project(multicore_engine_load_unit_gen)
file(GLOB_RECURSE LOAD_UNIT_GEN_SRC "src/*.cpp")
file(GLOB_RECURSE LOAD_UNIT_GEN_HEADERS "include/*.hpp")
add_executable(multicore_engine_load_unit_gen ${LOAD_UNIT_GEN_SRC} ${LOAD_UNIT_GEN_HEADERS})
make_src_groups()
target_link_libraries(multicore_engine_load_unit_gen multicore_engine_base)
if(MSVC)
	set_target_properties(multicore_engine_load_unit_gen PROPERTIES LINK_FLAGS_RELEASE "/LTCG")
	set_target_properties(multicore_engine_load_unit_gen PROPERTIES LINK_FLAGS_MINSIZEREL "/LTCG")
	set_target_properties(multicore_engine_load_unit_gen PROPERTIES LINK_FLAGS_RELWITHDEBINFO "/LTCG")
endif()
